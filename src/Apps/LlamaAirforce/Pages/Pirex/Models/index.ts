export type Reward = {
  symbol: string;
  address: string;
  amount: number;
  amountUsd: number;
};

export type Redemption = {
  symbol: string;
  address: string;
  amount: number;
  amountUsd: number;
};

export type Staking = {
  symbol: string;
  address: string;
  amount: number;
  amountUsd: number;
};
