import { type App } from "@/Framework/Apps";

export const hosts: Record<App, string | undefined> = {
  laf: "https://api.llama.airforce",
  cb: undefined,
  cm: "https://api.llama.airforce",
  pm: undefined,
  "pm-lrt": undefined,
};
